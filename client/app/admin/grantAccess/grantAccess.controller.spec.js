'use strict';
/*
describe('Controller: GrantaccessCtrl', function () {

  // load the controller's module
  beforeEach(module('roadAmicoApp'));

  var GrantaccessCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    GrantaccessCtrl = $controller('GrantaccessCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });
});
*/