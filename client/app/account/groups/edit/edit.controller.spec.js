'use strict';

describe('Controller: EditGroupCtrl', function () {

  // load the controller's module
  beforeEach(module('roadAmicoApp'));

  var EditCtrl, scope;

  // Initialize the controller and a mock scope
  //beforeEach(inject(function ($controller, $rootScope) {
  //  scope = $rootScope.$new();
  //  EditCtrl = $controller('EditGroupCtrl', {
  //    $scope: scope
  //  });
  //}));

  it('should ...', function () {
    expect(1).toEqual(1);
  });
});
