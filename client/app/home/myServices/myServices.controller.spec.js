'use strict';

describe('Controller: MyservicesCtrl', function () {

  // load the controller's module
  beforeEach(module('roadAmicoApp'));

  var MyservicesCtrl, scope;

  // Initialize the controller and a mock scope
  //beforeEach(inject(function ($controller, $rootScope) {
  //  scope = $rootScope.$new();
  //  MyservicesCtrl = $controller('MyservicesCtrl', {
  //    $scope: scope
  //  });
  //}));

  it('should ...', function () {
    expect(1).toEqual(1);
  });
});
